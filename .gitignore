# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor
.vscode/
.idea/

# Figma export (large SVG, not needed in repo)
*.svg

# Raw/unoptimized source files (keep original exports local only)
# SRC/ images are committed in optimized form

# Astro / npm (dashboard experiment — see CLAUDE.md)
node_modules/
dist/
.astro/

# Python bytecode cache (uipro skill scripts)
__pycache__/
*.pyc
